<!DOCTYPE html>

<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Integration Process</title>

    <!-- CSS link (loading Semantic UI from a CDN) -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/semantic-ui/2.4.1/semantic.min.css" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <style>
        body { padding:50px; }
    </style>
</head>

<body class="container">
<h3><%= data.title%></h3>
<% for (const item of data.csvFiles) {%>

    <p><b>File name:</b> <%= item.fileName %></p>

    <% if (item.errorMessage) {%>
        <p><b>Message:</b> <%= item.errorMessage %></p>
    <%}%>
<%}%>


</body>

</html>